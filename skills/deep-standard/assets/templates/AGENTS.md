# AGENTS.md

<!-- 这是常驻层：每个 session 都必须进入上下文的规则。每条 1–3 行、自包含、链接它的家。不放故事、示例、情境流程、任何从被链接的家里复述来的内容。 -->

<!-- 一句话说清项目是什么 + 指向改动前必读的地图 -->
<项目一句话定位>。改动 `<核心目录>` 前先读 [<架构地图>](docs/architecture.md)；文档规范见 [docs/AGENTS.md](docs/AGENTS.md)。

## 当前阶段取舍

<!-- 元法则 B：显式声明当前阶段的取舍，并在同一节里写明失效条件。没有失效条件的取舍声明会变成永久的技术债借口。示例见下，按项目实际改写。 -->
<!-- 示例（发布前）：**首个正式版本发布时删除本节。** 尚无外部消费者，因此优先选择正确的基础形态而非兼容性垫片：可以自由重命名与重组，但必须同一个改动内更新全部引用。 -->
<!-- 示例（已发布）：**本节长期有效。** 已有外部消费者，破坏性改动需要迁移路径与弃用周期，见 <链接>。 -->

## 目录布局

```
<!-- 一屏内说清每个顶层目录的职责，一行一个 -->
src/          <职责>
tests/        <职责>
scripts/      <门禁与生成器；清单在 scripts/<编排器>>
docs/         <文档；标准见 docs/AGENTS.md>
decisions/    <决策记录；契约见 decisions/README.md>
```

## 命令

```sh
<!-- 只列日常真的会用的；完整清单指向 package.json / Makefile / 门禁编排器 -->
<安装命令>
<测试命令>              # 单元测试
<类型检查命令>
<lint 命令>
<门禁命令>              # 全部门禁；清单的权威在 <编排器路径>
<构建命令>
```

### 按改动面选检查

改动落在哪些表面，就跑覆盖那些表面的**最窄**证据，跑一次；只汇报真正跑过的命令。**不要默认跑全套**，不要为了提交或推送重复跑已经通过的检查——CI 独占穷尽覆盖与平台矩阵。

| 改动触达 | 跑什么 |
|---------|--------|
| <某模块行为> | <拥有它的测试文件 / 按测试名过滤> |
| 文档、决策记录、生成物 | <文档门禁组命令> |
| <可见输出/协议> | <对应快照场景> |
| 包清单 / 公开导出 / 构建配置 | <构建 + 卫生检查 + 产物冒烟> |

## 约定

<!-- 每条 1–3 行、可判定（能对着 diff 回答是否违规）、有边界、命名活着的机制（不说"应该"，说"是"）。下面几条是本体系要求的最小集；跨语言可移植的候选约定见 references/standing-orders.md「可移植的约定候选清单」，逐条评估后择取，其余按项目补充。 -->

- **每个非平凡改动必须在同一 PR 内新增或更新至少一条决策记录**；豁免范围与格式见 [decisions/README.md](decisions/README.md)。
- **约定必须有门禁。** 机械可查的约定写成只拒绝一条不变量的程序；每个门禁必须有用非法样例证明它会拒绝的测试。门禁清单的权威是 `<编排器路径>`，本文件不手抄。
- **生成物一律不手改**，改生成器；生成物由 `--check` 模式守护新鲜度。
- **注释与文档写契约，不写推理转录**：行为、时序、异常、后果、所有权、安全使用条件保留；实现叙述、测试走查、评审史、代码复述删除。只写当前态，变更史进 commit/PR/决策记录。
- **空的异常捕获必须写明它吞掉了什么、以及为什么不会有别的东西到达这里**，且 `try` 只包一条语句。
- **随部署环境变化的选择必须是经校验的配置字段**，不是常量或测试钩子；协议常量、外部规范、安全不变量保持固定。
- **配置错误要响亮地失败**：能自包含判定的在加载时失败，否则在最早可判定处失败；绝不静默跳过缺失的引用目标。
- **测试描述行为，不描述正确性。** 行为过时就连同它的测试一起改，并在 PR 里说明为什么。
- 待办标记按紧急度：`FIXME`（阻塞发布）/ `TODO`（尽快）/ `XXX`（也许哪天）。
- **一个 PR 一种性质**（feature / bug-fix / doc / testing / cleanup）+ 对应标签；独立改动拆开；缺陷在引入它的那个 PR 上修，不往下游打补丁。
- **未被明确要求时不推送、不合并、不发布**；不可逆动作需要针对该具体动作的显式批准。
- <项目特有约定……>

## 编辑本文件

<!-- 如果本文件被软链或镜像到别的路径（CLAUDE.md 等），在此说明"编辑真实文件" -->
每条规则保持自包含，同时链接高层文档。能压缩就压缩<!-- L3：；确实需要更多空间时才抬 <预算 manifest 路径> 的上限，并在 PR 里给理由 -->。
